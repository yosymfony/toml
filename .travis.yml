language: php

php:
  - 7.1
  - 7.2
  - nightly

before_script:
  - composer self-update
  - composer install --no-interaction --no-dev
  
script:
  - ./vendor/bin/phpunit --colors=always

matrix:
  allow_failures:
    - php: nightly
  fast_finish: true
